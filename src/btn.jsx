const BtnAdd = (props)=>{
    return (
           <button className="border-[1px] px-2 py-1 text-md rounded-md hover:bg-blue-600 duration-300 '" onClick={props.GetImages}>
            Generate
        </button>
    )
}

export default BtnAdd;